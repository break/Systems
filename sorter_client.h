#ifndef SORTER_CLIENT_H_
#define SORTER_CLIENT_H_

#include <stdio.h> 
#include <unistd.h> 
#include <string.h> 
#include <stdlib.h> 

#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <netdb.h>

#include <sys/types.h>

/* Define default port number and address: STDIN will specify it. Make sure it is valid (80 is reserved) */ 

#endif